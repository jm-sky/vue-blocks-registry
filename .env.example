# Use mock auth service for demo (no real backend needed)
# Set to 'false' to use real API backend
VITE_USE_MOCK_AUTH=true

# API Base URL (only used when VITE_USE_MOCK_AUTH=false)
VITE_API_BASE_URL=http://localhost:3000/api

# App Configuration
VITE_APP_ID=vue-blocks-registry
VITE_APP_NAME=Vue Blocks Registry
VITE_APP_DESCRIPTION=Vue component blocks registry and demo application
